{
  "name": "dlightrag-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "test": "node --experimental-strip-types --test stores/*.test.ts lib/*.test.ts",
    "typecheck": "tsc --noEmit",
    "lint:css": "stylelint 'styles/**/*.css' 'styles/**/*.module.css'"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "stylelint": "^17.14.1",
    "stylelint-config-standard": "^40.0.0",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  },
  "dependencies": {
    "dompurify": "^3.4.12",
    "htmx.org": "^2.0.10",
    "nanoevents": "^10.0.0"
  }
}
